{
  "poolId": "L1.12.items",
  "conceptId": "L1.12",
  "conceptName": "Thresholds & resumability",
  "items": [
    {
      "id": "L1.12.q1",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "A feed that should return 65,000 students has a bug and returns 0 records. What feature prevents the destination's entire student set from being garbage-collected (deleted)?",
      "options": [
        { "id": "a", "text": "Delta detection" },
        { "id": "b", "text": "A garbage-collection / deletion percentage abort threshold" },
        { "id": "c", "text": "The duplication key" },
        { "id": "d", "text": "Resumability" }
      ],
      "correct": ["b"],
      "explanation": "The GC/deletion percentage threshold compares the would-be deletions (all 65,000) against the destination count; exceeding the configured % aborts the run.",
      "misconceptions": {
        "a": "Delta detection would faithfully conclude everything was deleted — the threshold is the guard that stops it.",
        "d": "Resumability handles crashes, not bad data volumes."
      },
      "source": "Documentation/ProposedAbortThresholdOptions.docx.md"
    },
    {
      "id": "L1.12.q2",
      "type": "mc",
      "difficulty": "recall",
      "scoring": "auto",
      "stem": "What does resumability guarantee?",
      "options": [
        { "id": "a", "text": "Runs always complete faster on the second attempt" },
        { "id": "b", "text": "If the server crashes mid-run, no data is lost and the next run picks up where it left off" },
        { "id": "c", "text": "The destination is backed up before every run" },
        { "id": "d", "text": "Exceptions are automatically retried forever" }
      ],
      "correct": ["b"],
      "explanation": "Because progress is recorded in StarBridge, a crash mid-run causes no data loss; the next run resumes from where it stopped.",
      "misconceptions": {
        "c": "Resumability is about safe continuation, not backups."
      },
      "source": "README.md#4-key-features-worth-knowing"
    },
    {
      "id": "L1.12.q3",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "If the deletion-percentage threshold parameter is omitted from configuration, what is assumed?",
      "options": [
        { "id": "a", "text": "0% — no deletions are ever allowed" },
        { "id": "b", "text": "100% — full deletion is allowed (no deletion guard)" },
        { "id": "c", "text": "5% by default" },
        { "id": "d", "text": "The run will not start" }
      ],
      "correct": ["b"],
      "explanation": "An omitted percentage is assumed to be 100%, meaning the destination entity could be fully deleted — the guard only protects you if you configure it.",
      "misconceptions": {
        "a": "It defaults to permissive (100%), not restrictive (0%)."
      },
      "source": "Documentation/ProposedAbortThresholdOptions.docx.md"
    },
    {
      "id": "L1.12.q4",
      "type": "mc",
      "difficulty": "recall",
      "scoring": "auto",
      "stem": "What is the difference between the SegmentAbortThreshold and the RunAbortThreshold?",
      "options": [
        { "id": "a", "text": "One counts exceptions within a single segment; the other is the limit across the whole run" },
        { "id": "b", "text": "One is for deletions; the other is for creations" },
        { "id": "c", "text": "They are the same thing with different names" },
        { "id": "d", "text": "One applies to the source; the other to the destination" }
      ],
      "correct": ["a"],
      "explanation": "SegmentAbortThreshold aborts a single segment after N exceptions; RunAbortThreshold is the overall exception limit for the entire run.",
      "source": "README.md#54-how-a-run-is-wired-together-appsettingsjson"
    },
    {
      "id": "L1.12.q5",
      "type": "mc",
      "difficulty": "synthesis",
      "scoring": "auto",
      "stem": "Thresholds exist because delta detection trusts the snapshot. What is the best one-sentence framing of what thresholds are?",
      "options": [
        { "id": "a", "text": "A way to make runs finish faster" },
        { "id": "b", "text": "Guardrails against a bad snapshot, so a faulty feed can't silently wreck the destination" },
        { "id": "c", "text": "A logging feature for the dashboard" },
        { "id": "d", "text": "A replacement for mappings" }
      ],
      "correct": ["b"],
      "explanation": "Because the engine acts on whatever snapshot the feed provides, thresholds are the safety guardrails that abort the run when the snapshot implies dangerous, large-scale change.",
      "source": "README.md#4-key-features-worth-knowing"
    },
    {
      "id": "L1.12.q6",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "How does the orphan-mapping abort threshold differ from the garbage-collection (deletion) abort threshold?",
      "options": [
        { "id": "a", "text": "The orphan-mapping threshold is checked preemptively during the Migration segment — before any GC runs; the GC/deletion threshold is checked during the GarbageCollection segments" },
        { "id": "b", "text": "They are the same threshold with two names" },
        { "id": "c", "text": "The orphan-mapping threshold only counts exceptions, not removals" },
        { "id": "d", "text": "The GC threshold is checked first, then the orphan-mapping threshold" }
      ],
      "correct": ["a"],
      "explanation": "Both guard against a bad feed causing mass removal, but the orphan-mapping check is preemptive: as the Migration identifies mappings whose source record is gone (slated for deactivation/removal), it aborts before GC deletes anything. The GC/deletion percentage threshold fires during the GarbageCollection segments themselves.",
      "misconceptions": {
        "b": "They fire at different points — orphan-mapping during the migration (preemptive), GC during the GC segments.",
        "c": "The orphan-mapping threshold is about mappings identified for removal, not exception counts (those are the Segment/Run abort thresholds).",
        "d": "The orphan-mapping check comes first — it's preemptive, before GC removes anything."
      },
      "source": "lessons/L1.12.md - orphan-mapping preemptive abort example"
    },
    {
      "id": "L1.12.q7",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "How should you set the orphan-mapping and garbage-collection abort percentages, and which is the more valuable guard?",
      "options": [
        { "id": "a", "text": "Set both to the same percentage; the orphan-mapping threshold is more valuable because it fires preemptively during migration, aborting before any deletion so you can just re-run" },
        { "id": "b", "text": "Set the GC percentage far lower than the orphan one; the GC threshold is the only real guard" },
        { "id": "c", "text": "Set the orphan percentage to 100% and rely on the GC threshold" },
        { "id": "d", "text": "They must never be equal or the run won't start" }
      ],
      "correct": ["a"],
      "explanation": "Both guard the same failure (a bad feed causing mass removal), so a single consistent limit is what you want. The orphan-mapping check is preemptive — it trips first, during migration, before any deletion — so it's the more beneficial to rely on and it leaves a clean re-run; the matching GC value is the backstop.",
      "misconceptions": {
        "b": "The orphan-mapping threshold is the front-line preemptive guard; the GC threshold is the later backstop.",
        "c": "Setting orphan to 100% disables the preemptive guard, forcing reliance on the later GC abort — after deletion has begun.",
        "d": "Equal values are exactly what's recommended, so the removal limit is consistent across both checks."
      },
      "source": "lessons/L1.12.md - orphan vs GC threshold config"
    }
  ]
}
