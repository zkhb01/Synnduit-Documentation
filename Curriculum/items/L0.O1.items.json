{
  "poolId": "L0.O1.items",
  "conceptId": "L0.O1",
  "conceptName": "Interfaces & implementation",
  "items": [
    {
      "id": "L0.O1.q1",
      "type": "mc",
      "difficulty": "recall",
      "scoring": "auto",
      "stem": "When a class implements an interface, how many of the interface's members must it provide?",
      "options": [
        { "id": "a", "text": "Any subset it chooses" },
        { "id": "b", "text": "All of them" },
        { "id": "c", "text": "Only the properties, not the methods" },
        { "id": "d", "text": "None — the interface provides default bodies for everything" }
      ],
      "correct": ["b"],
      "explanation": "An interface is a contract; the implementing class must supply every member (the compiler enforces it).",
      "misconceptions": {
        "a": "Picking a subset would break the contract and fail to compile."
      },
      "source": "https://learn.microsoft.com/dotnet/csharp/fundamentals/types/interfaces"
    },
    {
      "id": "L0.O1.q2",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "What is the role of `ISink<T>` in Synnduit?",
      "options": [
        { "id": "a", "text": "A concrete class that writes to the destination" },
        { "id": "b", "text": "A contract that your destination connector class must fulfill" },
        { "id": "c", "text": "A configuration setting in appsettings.json" },
        { "id": "d", "text": "A StarBridge table" }
      ],
      "correct": ["b"],
      "explanation": "ISink<T> is an interface (a contract); your sink class implements it, supplying all its members.",
      "misconceptions": {
        "a": "ISink<T> is the contract; the concrete class (e.g. StaffSink) is what implements it."
      },
      "source": "ISink<T> member list mapped to a real sink"
    },
    {
      "id": "L0.O1.q3",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "Why can the Synnduit engine call any connector you write without knowing your concrete class?",
      "options": [
        { "id": "a", "text": "It reads your source code at runtime" },
        { "id": "b", "text": "It talks to your class through the interface (IFeed<T> / ISink<T>) it implements" },
        { "id": "c", "text": "All connectors must be named the same" },
        { "id": "d", "text": "It copies your class into the engine" }
      ],
      "correct": ["b"],
      "explanation": "Because your class fulfills the interface contract, the engine can call it polymorphically through that interface — that's the value of programming to an interface.",
      "source": "ISink<T> member list mapped to a real sink"
    },
    {
      "id": "L0.O1.q4",
      "type": "short",
      "difficulty": "apply",
      "scoring": "model",
      "stem": "A teammate writes `class StaffSink : ISink<Staff>` but only implements Create and Update. What will happen and why?",
      "rubric": [
        "It will not compile",
        "Because ISink<Staff> is a contract requiring all its members (New/Create/Get/Update/Delete) to be implemented"
      ],
      "sampleAnswer": "It won't compile — implementing ISink<Staff> obligates the class to provide every member of the interface, not just Create and Update. The missing members are a compile error.",
      "source": "https://learn.microsoft.com/dotnet/csharp/fundamentals/types/interfaces"
    },
    {
      "id": "L0.O1.q5",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "StaffSink and StudentSink each implement ISink<T>. Why can the engine drive both with the same code?",
      "options": [
        { "id": "a", "text": "They share the ISink<T> contract, so the engine calls them polymorphically through that interface" },
        { "id": "b", "text": "They must inherit from a common base class" },
        { "id": "c", "text": "Their class names must match a naming pattern" },
        { "id": "d", "text": "The engine recompiles them at runtime" }
      ],
      "correct": ["a"],
      "explanation": "Programming to the interface lets the engine treat any implementer uniformly, without knowing the concrete class.",
      "misconceptions": {
        "b": "Interfaces don't require a shared base class — fulfilling the contract is enough."
      },
      "source": "ISink<T> member list mapped to a real sink"
    },
    {
      "id": "L0.O1.q6",
      "type": "mc",
      "difficulty": "apply",
      "scoring": "auto",
      "stem": "Can one class implement both `ISink<Staff>` and `ICacheFeed<Staff>` (as SinkAndCacheFeed does)?",
      "options": [
        { "id": "a", "text": "Yes — a class may implement multiple interfaces, supplying the members of each" },
        { "id": "b", "text": "No — a class can implement only one interface" },
        { "id": "c", "text": "Only if the two interfaces share a base interface" },
        { "id": "d", "text": "Only by inheriting from both as base classes" }
      ],
      "correct": ["a"],
      "explanation": "A class can fulfill several interface contracts at once — that's how one class can be both the sink and the cache feed.",
      "misconceptions": {
        "b": "Single-interface-only is a misconception; multiple interfaces are allowed (and common here)."
      },
      "source": "SinkAndCacheFeed.cs"
    }
  ]
}
