{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Hosting": {
    "_comment": "Sub-path the app is served under when hosted behind another site (e.g. SDM's IIS site at /Tutor). Set PathBase to '/Tutor' in production; leave empty to serve at the site root for local dev.",
    "PathBase": ""
  },
  "Courses": [
    {
      "_comment": "Training courses. Path is the curriculum folder: an existing absolute path, or a folder name located by walking up from the app (so 'Curriculum' and 'Curriculum-SDM' resolve as sibling folders without machine-specific paths). With this array empty/absent the app falls back to a single 'synnduit' course resolved by walking up for a Curriculum/ folder.",
      "Id": "synnduit",
      "Name": "Synnduit Tutorial",
      "Description": "Learn how Synnduit works, one concept at a time.",
      "Path": "Curriculum"
    },
    {
      "Id": "sdm",
      "Name": "SDM Tutorial",
      "Description": "Learn the Supplemental Data Manager — what it is, how it's built, and how access works.",
      "Path": "Curriculum-SDM"
    }
  ],
  "Anthropic": {
    "_comment": "Live Claude remediation. Leave ApiKey empty here; supply it via the ANTHROPIC_API_KEY environment variable or dotnet user-secrets so it never lands in source control. With no key, remediation falls back to the deterministic StubRemediationService.",
    "ApiKey": "",
    "MaxTokens": 8000
  },
  "AzureAd": {
    "_comment": "Microsoft Entra ID (Azure AD) SSO. Fill TenantId + ClientId to enable Entra sign-in; leave ClientId empty to keep the local stub sign-in. Put ClientSecret in user-secrets/env, never here. In the app registration, add a Web redirect URI of https://<host>/signin-oidc and a front-channel logout URL of https://<host>/signout-callback-oidc.",
    "Instance": "https://login.microsoftonline.com/",
    "Domain": "",
    "TenantId": "",
    "ClientId": "",
    "ClientSecret": "",
    "CallbackPath": "/signin-oidc",
    "SignedOutCallbackPath": "/signout-callback-oidc"
  },
  "Donuts": {
    "ManagerName": "<Manager Name>",
    "RedeemThresholdPoints": 6,
    "Award": {
      "ConceptMastered": 1,
      "FirstAttemptBonus": 1,
      "PerfectScoreBonus": 1,
      "CriticalConceptBonus": 1,
      "LevelCompletionBonus": 3
    }
  }
}
